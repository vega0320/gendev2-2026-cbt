# 생성발2 CBT 프로젝트 규칙

이 파일은 이 프로젝트 전체에 적용한다. 작업을 시작할 때 루트 `AGENTS.md`,
`HTML_학습자료_재사용_설계노트.md`, 이 파일을 순서대로 읽는다.

## 사이트 정체와 현재 기준

- 서비스: 생성발2 강의별 CBT
- 배포 링크: `https://gendev2-2026-cbt.pages.dev/`
- 기존 통합 사이트 링크: `https://vega0320-med-quiz.pages.dev/`
- GitHub: `https://github.com/vega0320/gendev2-2026-cbt`
- 현재 기준 데이터: 42개 강의 탭(40강은 40-1·40-2로 분리), 499문항. 2026 예상문제 30개, 강의록 과거문제 17개,
  강의 제시 연습문제 2개를 포함한다. 개수 변경 시 데이터·검사·이 문서를 함께 고친다.
- 문제 원문은 텍스트 CBT로 표시한다. 원본 표와 사진은 모두 이미지로 보존하고
  문항 ID의 명시적 `assets`/`assetCrops` 목록으로 연결한다.
- 예상문제는 `예상문제 · 비출제` 표지를 사용한다. 실제 출제문항처럼 보이게 하지 않는다.
- 2026 교수명은 강의 목록과 교수진 탭에 표시한다. 문제 카드에는 반복 태그를 붙이지 않는다.
- 강의 목록은 접고 펼칠 수 있어야 하며 `동일 교수 문항/전체 문항`을 표시한다.
- 교수진 탭은 족보 앞부분의 연도별 담당교수 표를 보존하고 2026 및 동일 교수를 강조한다.

## 사용자 상태와 공유 기능

- 출석번호를 첫 화면에서 입력한다. 풀이·시도·누적 오답·모름·문항별 메모는
  같은 출석번호로 데스크톱과 모바일에서 동기화한다.
- 출석번호가 다르면 개인 상태를 섞지 않는다. 서버에는 문제 본문을 복사하지 않고
  안정적 문항 ID와 상태만 저장한다.
- Cloudflare D1의 풀이 진행 API를 정본으로 사용하고 로컬 저장소는 오프라인 캐시로 쓴다.
  충돌 시 `updatedAt`이 최신인 문항 상태를 보존한다. 배포 뒤 교차기기 검사를 반드시 한다.
- 오류토의는 문항별 공유 공간이며 모든 출석번호에서 실시간으로 보인다.
- 메모는 문항 ID별로 분리하고 풀이 화면과 틀림/모름 탭에서 편집·확인할 수 있게 한다.
- 강의 목록의 짧은 메모는 `lectureNotes[강의번호]`로 문항 메모와 분리하고 80자 이내로 저장한다.
  같은 출석번호에서는 서버와 로컬 캐시를 병합해 다른 기기에서도 이어져야 한다.
- 풀이현황 탭은 강의별 총 문항, 푼 문항, 정답, 현재 오답, 모름, 누적 시도를 집계한다.
- 틀림/모름 탭에서는 해당 문제를 다시 풀 수 있어야 한다. 개념 탭은 현재 오답·모름의
  `keyConcepts`를 매번 다시 계산하며 메모를 함께 보여 준다.
- 틀림/모름 탭에는 개별 다시 풀기와 `전체 연속 풀기`를 함께 둔다. 연속 풀기는 시작 시점의
  복습 문항 ID를 고정된 순서로 묶어 이전·다음 이동을 제공한다. 풀이 중 정답 처리되어 복습 목록에서
  빠진 문항도 현재 연속 세트에서는 갑자기 사라지지 않아야 한다.
- 개념 정리 탭은 개념명과 문항 링크만 나열하지 않는다. 현재 복습 문항에서 핵심 판단, 본과 개념,
  진단기준·분류, 정상치·수치기준, 자주 틀리는 지점, 개인 메모를 추출해 묶어 보여 준다.
  같은 문항 집합에서 나온 여러 `keyConcepts`는 한 카드로 합쳐 동일 내용 카드가 반복되지 않게 한다.
- 같은 개념·유형 문항은 `similarGroupId`, `similarGroupPosition`, `relatedIds`로 연속 배치한다.
  임추·국시의 유사 문제는 별도 출처 표지와 함께 `relatedExternalIds`로 연결한다.
- 문제 검색은 검색어가 있으면 전체 강의의 문제·선지·개념을 찾고, 검색어가 없으면 현재 강의만 보여 준다.
  `올해와 동일 교수 문제만`은 `importance="high"`인 과거 문항만 남긴다.
- 풀이현황 막대의 길이는 완료율, 색은 현재 정답률을 뜻한다. 오답률 50% 이상은 밝은 빨강 계열로
  고정하고, 오답률 50% 미만 구간은 밝은 노랑에서 초록으로 연속 보간한다. 갈색·어두운 황토색은 쓰지 않는다.

## 안정적 데이터 규칙

- 문항 ID는 공개 후 바꾸지 않는다. UI 버튼, 저장 키, 토론 키, 유사문항 연결이 모두 같은 ID를 쓴다.
- 원본 문제, 선지, 정답, 해설, 강의, 출처 페이지, 이미지, 분류 상태를 구조화해 보관한다.
- 족보 정답 확인과 의학 해설 검수는 별도 상태다. 충돌·불명확 답은 자동 덮어쓰지 않는다.
- 강의 오분류가 확인되면 추정 실제 강의로 옮기고, 옮긴 강의에서 유사문항 묶음을 다시 계산한다.
- 새 문항을 한 개라도 추가한 뒤에는 반드시 `scripts/apply_similarity_groups.py`를 다시 실행한다.
  이 스크립트는 `내용상 NN강`이 명시된 오분류 의심 문항을 먼저 실제 강의로 옮기고,
  41강 전체의 문장·선지 유사도를 다시 계산해 `studyOrder`와 관련 문항을 갱신한다.
  실행 뒤 `오분류 의심` 상태가 하나라도 남으면 배포하지 않는다.
- 문제 요구(옳은 것/옳지 않은 것/복수정답)와 저장 답 개수는 내부 검사하되 화면에 검수 문구로 노출하지 않는다.

## 해설 품질: 절대 생략 금지

1. 각 문항을 실제로 다시 읽고 긍정형·부정형·복수정답 여부를 확인한 뒤 해설한다.
2. 순서는 `핵심 판단 → 한 단계씩 풀이 → 선지 위치별 해설 → 진단기준·수치기준 →
   본과 개념 복습 → 최신 기준상 주의점 → 출처`로 한다.
3. 선지별 해설은 그 선지의 의학적 주장을 직접 설명한다. 맞는 이유 또는 틀린 이유,
   그리고 어떤 조건에서 그 선지가 맞아지는지를 구체적으로 쓴다.
4. 다음과 같은 빈 문구는 금지한다.
   - `결정 단서와 맞지 않는다`
   - `관련되지 않는다`
   - `구분해야 한다`
   - `사례를 그 원칙에 대입한다`
   - `정답 조건과 맞지 않는다`
   - 정답 문장을 모든 오답에 그대로 복사한 뒤 `따라서 오답`이라고만 쓰는 방식
5. 같은 해설을 다른 문제에 복사하지 않는다. 유사문항이라도 달라진 수치, 임신주수,
   태아상태, 문제 요구, 정답 선지를 명시해 차이를 설명한다.
6. 선지별 해설에는 해당 선지의 문장과 의학적 사실만 쓴다. 모든 선지에 핵심 해설이나
   정답 선지 문장을 반복하지 않는다. 한 문항 안에서 동일 문장을 재사용하거나, 1번 해설에
   2~5번 선지 원문이 들어가면 자동검사를 실패시킨다.
7. 복수정답은 각 선지를 독립적으로 판정한다. `저장 답 개수`에 맞추려고 의학적으로
   맞는 선지를 억지로 제외하지 않는다. 족보 답과 현재 근거가 충돌하면 검수대기로 남긴다.
8. 선지별 해설은 풀이 때 보였던 선지 바로 아래에 나타나야 한다. 별도의 긴 해설 끝으로 몰지 않는다.
9. 정확히 같은 문자열만 찾는 검사는 충분하지 않다. 선지명과 `정답/오답` 표지를 제거한 본문끼리
   단어·문장 유사도를 비교한다. 말만 바꿔 같은 결론을 반복한 선지 쌍도 검수 실패로 본다.
10. 한 문항의 다섯 해설은 각각 다른 한 줄 주장으로 줄일 수 있어야 한다. 각 선지는 가능한 범위에서
    `무엇/기전 → 실제 적응증 또는 기준 → 이 증례에서의 판정 → 맞아지는 조건`을 설명한다.
    다섯 선지에 `1차 치료가 아니다`, `적응증이 아니다`, 정답 선지의 근거만 반복해서는 안 된다.
11. 검사·치료 선지가 서로 비슷하면 이름만 바꾸지 말고 기전, 검사 대상, 시행 시점, 위험, 다음
    의사결정 중 적어도 하나를 서로 다르게 설명한다. 계산형 선지는 각 오답이 어느 항목을 누락·과다계산했는지 적는다.
12. `fact_for` 같은 생성 함수의 공통 fallback으로 해설을 완성하지 않는다. 처리하지 못한 선지는
    예외를 발생시켜 빌드를 중단하고 해당 선지의 의학적 사실을 별도로 작성한다.
13. 18강 장중첩 치료 문항을 의미 중복 검사의 기준 시험판으로 유지한다. 공기정복, 바륨관장,
    초음파 유도 관장/외부 수기압박, 복강경, 개복·장절제는 각각 기전과 적응증이 달라야 한다.

## 진단기준과 수치기준

- 강의 단위 기본 수치표를 모든 문항에 붙이지 않는다. `numericReference`와
  `diagnosticCriteria`는 해당 문항의 판단에 직접 필요한 내용만 넣는다.
- 나이·연도·문항번호처럼 단순히 숫자가 있다는 이유로 수치 상자를 만들지 않는다.
- 수치가 정답을 가르면 정상범위, 진단 절단값, 중증도, 검사 간격, 처치·분만 시기를
  해당 문항에 맞춰 적는다. 단위와 적용 대상(임신주수, 수유 여부 등)을 생략하지 않는다.
- 수치기준이 필요 없는 문제는 빈 배열과 `numericReview.status = not-applicable`을 저장한다.
  필요한 문제는 `applicable`이며 비어 있으면 검사를 실패시킨다.
- 서로 다른 개념의 문항에 동일한 진단·수치 배열이 붙으면 검수 실패로 본다.
  같은 유사문항 묶음에서 실제로 같은 기준을 묻는 경우만 허용한다.
- 기준이 시대에 따라 바뀌면 교과서의 서술, 출제 당시 기준, 현재 공식 지침을 나누어 쓴다.

## 의학 근거와 저작권

- 교과서 설명은 Williams Obstetrics 등 허용된 교과서와 로컬 Harrison/홍창의 지식베이스를
  이용해 자체 문장으로 작성한다. 최신성 있는 수치·진단·치료는 공식 학회·정부 지침과
  원 논문으로 다시 확인한다. OpenEvidence는 탐색용이며 단독 정본으로 쓰지 않는다.
- 출처에는 근거 종류, 문서명, 연도/확인일, URL을 분리한다. 서로 다른 근거를 한 문장에 섞지 않는다.
- 공개 저장소와 사이트에 저작권 교과서 원문, 페이지 이미지, 표·그림을 복사하지 않는다.
  자체 작성한 해설과 출처 메타데이터만 배포한다.
- 문제 원본 표·사진은 사용자가 제공한 자료의 이미지로 유지하며 누락·잘림을 실제 화면에서 확인한다.

## 데이터 갱신 순서

1. 소스/강의록 추출 및 분류
2. 과거문항·2026 예상문제 적용
3. 해설 품질·최신 근거 보강
4. 오분류 재배치와 전체 유사문항 재계산: `scripts/apply_similarity_groups.py`
5. 1~13강 독립 선지 검수: `scripts/review_lectures_01_13.py`
6. 14~20강 독립 선지 검수: `scripts/review_lectures_14_20.py`
7. 1~20강 의미 중복 검사: `scripts/audit_choice_semantic_overlap.py --data data/questions.json --fail`
8. `scripts/prepare_site.py`
9. 자동검사와 실제 브라우저 검사
10. GitHub main push 후 Cloudflare Pages 배포

뒤 단계가 앞 단계의 수동 해설을 덮어쓰지 않게 한다. 배치 스크립트는 반복 실행해도 같은 결과가 나와야 한다.

## 완료 전 필수 검사

```powershell
python scripts/apply_similarity_groups.py
python scripts/review_lectures_01_13.py
python scripts/review_lectures_14_20.py
python scripts/audit_choice_semantic_overlap.py --data data/questions.json --fail
python scripts/prepare_site.py
python scripts/audit_choice_semantic_overlap.py --data site/data/questions.json --fail
python scripts/evidence_content_audit.py
python scripts/validate_site.py
npm run check:js
npm run test:api
npm run test:browser
```

- ID 중복, 5개 선지, 정답 범위, 강의 분류, 이미지 경로, 유사문항 순서와 링크를 검사한다.
- 1~20강은 금지 문구 0개, 모든 객관식 선지 해설 5개, 문항 내 정확·의미 중복 0개,
  다른 선지 원문 혼입 0개, 수치 적용상태와 실제 배열 일치를 검사한다. 산과력 계산처럼
  구조가 반복될 수밖에 없는 문항도 각 선지의 오류 항목 조합이 서로 다른지 별도로 검사한다.
- 동일한 진단·수치 배열이 다른 개념에 복사되지 않았는지 검사한다.
- 데스크톱과 모바일을 실제 렌더링해 문제·사진·표, 선지 아래 해설, 접히는 강의 탭,
  교수명 태그 제거, 메모, 틀림/모름, 풀이현황, 예상문제 표지를 확인한다.
- 같은 출석번호를 두 브라우저 컨텍스트에서 사용해 교차기기 동기화를 검사한다.
- 모든 검사 통과 뒤 GitHub와 Cloudflare에 배포하고 두 공유 링크 및 남은 검수대기를 보고한다.
